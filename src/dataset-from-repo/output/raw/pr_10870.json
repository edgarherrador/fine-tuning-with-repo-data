{
  "number": 10870,
  "title": "PS-69087 Phase 7: project export, Excel error list, ResourceFile coverage",
  "body": "## Summary\n\nCloses Phase 7 of the PS-69087 FileShare coverage plan.\n\n### Tests added\n- **ProjectExportSystem.GetProjectExportPath** (#4) — three tests in \\ProjectExportIntegrationTests\\ (okToCreateIfMissing true/false, portfolio path resolution)\n- **ExportManager.ExportErrorList** (#109) — \\ExportErrorListWritesExcelToWebFileUploadDirectory\\ in \\ExportImportScenarioTests\\\n- **ResourceFile.ResolveAsync** (#44) — \\ResourceFileResolveAsyncFindsLinkedFileInFMSDirectory\\ in \\AnnotationExporterTests\\\n\n### Tracking\n- Updated \\docs/temp/tfs-prep-non-tc-files/coverage-progress.md\\\n- Added \\PS-69087-fileshare-methods.playlist\\ (force-added; \\*.playlist\\ is gitignored)\n\n### Test run\nFocused local run blocked by \\NU1101: TrimbleCloud.FileService\\ restore on this agent environment. CI should run the playlist classes.\n\n## Plan reference\nPhase 7 from ps-69087 coverage plan.\n\nMade with [Cursor](https://cursor.com)",
  "state": "open",
  "draft": false,
  "labels": [],
  "author": "AdamCollins",
  "created_at": "2026-05-25T21:15:33Z",
  "merged_at": null,
  "base_branch": "wip/adam/tfc-prep-non-tc-files",
  "base_sha": "00d30bfa0b36d7b70033279beac772b15b426254",
  "head_branch": "wip/adam/PS-69087-phase7-coverage",
  "head_sha": "d695117640f74cf1baf0eb30af5d82720bdfa559",
  "additions": 0,
  "deletions": 0,
  "changed_files": 0,
  "approvals_count": 0,
  "approvers": [],
  "changes_requested_count": 0,
  "reviews": [
    {
      "state": "COMMENTED",
      "body": "## Pull request overview\n\nCloses Phase 7 of the PS-69087 FileShare test-coverage plan by adding targeted integration tests for a few remaining FileShare-related helpers and capturing the tracking artifacts (playlist + progress doc) used to run/monitor that coverage work.\n\n**Changes:**\n- Added integration tests covering `ProjectExportSystem.GetProjectExportPath`, `ExportManager.ExportErrorList`, and `ResourceFile.ResolveAsync`.\n- Introduced a Visual Studio test playlist to run the Phase 7 test classes.\n- Added/updated a Phase checklist/progress tracker under `docs/temp/`.\n\n### Reviewed changes\n\nCopilot reviewed 5 out of 5 changed files in this pull request and generated 2 comments.\n\n<details>\n<summary>Show a summary per file</summary>\n\n| File | Description |\r\n| ---- | ----------- |\r\n| docs/temp/tfs-prep-non-tc-files/PS-69087-fileshare-methods.playlist | Adds a playlist that includes the Phase 7 test classes. |\r\n| docs/temp/tfs-prep-non-tc-files/coverage-progress.md | Adds Phase checklist/progress tracking and Phase 0 notes/evidence. |\r\n| Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/ProjectExport/ProjectExportIntegrationTests.cs | Adds `GetProjectExportPath` coverage and scratch FileShare directory helpers/cleanup. |\r\n| Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/PDF/AnnotationExporterTests.cs | Adds `ResourceFile.ResolveAsync` coverage via a file-link + FMSDirectory resolution scenario. |\r\n| Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/Excel/Submittals/ExportImportScenarioTests.cs | Adds `ExportErrorList` coverage asserting error workbook persistence and structure. |\n</details>\n\n\n\n\n\n",
      "author": "copilot-pull-request-reviewer[bot]",
      "submitted_at": "2026-05-25T21:20:25Z"
    }
  ],
  "review_comments": [
    {
      "path": "Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/ProjectExport/ProjectExportIntegrationTests.cs",
      "line": 30,
      "body": "`CustomTestCleanupAsync` calls `await base.CustomTestCleanupAsync()` first, but the integration test conventions require calling the base cleanup last so framework cleanup runs after any custom side-effect cleanup. Move the base call to the end of the method (after removing created requests and releasing scratch FileShare directories).",
      "author": "Copilot",
      "created_at": "2026-05-25T21:20:24Z",
      "in_reply_to_id": null
    },
    {
      "path": "Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/PDF/AnnotationExporterTests.cs",
      "line": 119,
      "body": "The `ResourceManager` in this test is created with a `DownloadRequestBase` that doesn't set `AsBuilt`/`AsBuiltIncludeFiles`. In production `ResourceFile.ResolveAsync` is invoked from `ResolveResourcesAsync` only for `request.AsBuilt` requests, and `FilesSubFolder`/zip-path setup is initialized only in that mode. Setting `AsBuilt = true` (and enabling the relevant include flags) or using the existing `GetResourceManager(...)` helper would make the test exercise the same configuration and avoid `FileZipPath` becoming null when the DMS folder is the root folder (no `\\` in the returned path).\n",
      "author": "Copilot",
      "created_at": "2026-05-25T21:20:25Z",
      "in_reply_to_id": null
    }
  ],
  "issue_comments": [],
  "files": [
    {
      "filename": "Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/Excel/Submittals/ExportImportScenarioTests.cs",
      "status": "modified",
      "additions": 40,
      "deletions": 0,
      "patch": "@@ -179,6 +179,46 @@ public async Task ExportUpdateRowsWithErrorsImportFixImport()\n             // Check the results\n             AssertReturnStatusCounts(oSecondImportResult, 2, 0, 2, 0, 0);\n         }\n+\n+        // PS-69087 Phase 7: covers ExportManager.ExportErrorList (test-coverage.md #109)\n+        // Forces an import error then asserts the regenerated error-list Excel\n+        // landed in the portfolio's WebFileUploadDirectory.\n+        [TestMethod]\n+        public async Task ExportErrorListWritesExcelToWebFileUploadDirectory()\n+        {\n+            var exportedFile = (await ExportRecordsToExcelAsync(SUBMITTAL_TABLE_TYPE, new List<int>())).ExportedFileName;\n+\n+            var workbook = ExcelFile.Load(exportedFile, LoadOptions.XlsxDefault);\n+            var worksheet = GetWorksheet(workbook, SUBMITTAL_TABLE_TYPE);\n+            var columnIndices = GetColumnIndices(worksheet);\n+\n+            // Add one row with a subject but no required revision/description so the import will fail.\n+            worksheet.Rows.InsertEmpty(FIRST_DATA_ROW_INDEX);\n+            worksheet.Rows[FIRST_DATA_ROW_INDEX].Cells[columnIndices[\"subject\"]].Value = Generate.String(\"ErrorListSubject\");\n+            worksheet.Rows[FIRST_DATA_ROW_INDEX].Cells[columnIndices[\"workflowstatename\"]].Value = WorkflowStateNames.Submittal.OPEN;\n+\n+            workbook.Save(exportedFile, SaveOptions.XlsxDefault);\n+\n+            var importResult = await ImportRecordsFromExcelAsync(exportedFile, SUBMITTAL_TABLE_TYPE);\n+\n+            importResult.Should().NotBeNull();\n+            importResult.HasErrors.Should().BeTrue(\"test seeds invalid rows so the importer must surface errors\");\n+            importResult.ErrorsFileName.Should().NotBeNullOrWhiteSpace(\"ExportErrorList must report the regenerated workbook name\");\n+\n+            var webFileUploadDirectory = GetWebFileUploadDirectory(m_oPortfolioId);\n+            var errorListPath = Path.Combine(webFileUploadDirectory, importResult.ErrorsFileName);\n+\n+            using (new AssertionScope())\n+            {\n+                File.Exists(errorListPath).Should().BeTrue(\"ExportErrorList must persist the error workbook to WebFileUploadDirectory\");\n+                Path.GetExtension(errorListPath).Should().Be(Support.EXCEL_EXTENSION, \"regenerated workbook must keep the .xlsx extension produced by GemBox Save\");\n+            }\n+\n+            var errorWorkbook = ExcelFile.Load(errorListPath, LoadOptions.XlsxDefault);\n+            var errorWorksheet = GetWorksheet(errorWorkbook, SUBMITTAL_TABLE_TYPE);\n+            errorWorksheet.Should().NotBeNull(\"regenerated workbook must contain the submittal worksheet\");\n+            errorWorksheet.Columns[0].Hidden.Should().BeFalse(\"ExportErrorList must un-hide the feedback column for failed rows\");\n+        }\n     }\n }\n "
    },
    {
      "filename": "Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/PDF/AnnotationExporterTests.cs",
      "status": "modified",
      "additions": 36,
      "deletions": 0,
      "patch": "@@ -99,6 +99,42 @@ public async Task UserCanSeeExportedAnnotationsWithRecordAndFileLinks()\n             ValidateResultPDF(returnResult, NUMBER_OF_ANNOTATIONS);\n         }\n \n+        // PS-69087 Phase 7: covers ResourceFile.ResolveAsync (test-coverage.md #44)\n+        [TestMethod]\n+        public async Task ResourceFileResolveAsyncFindsLinkedFileInFMSDirectory()\n+        {\n+            var drawing = await CreatePublishedDrawingWithAnnotations(DRAWING_TO_UPLOAD, 1);\n+            drawing = await AddFileLinkToAnnotation(drawing, drawing.Annotations[0]);\n+\n+            var fileLink = drawing.FileLinks.Single(l => l.AnnotationGUID == drawing.Annotations[0].GUID);\n+            var stagedUploadPath = Path.Combine(GetWebFileUploadDirectory(m_oPortfolioId), fileLink.FileGuid);\n+            File.Exists(stagedUploadPath).Should().BeTrue(\"AddFileLinkToAnnotation must stage the file in WebFileUploadDirectory\");\n+\n+            var requestedBy = GenerateRequestedByProject();\n+\n+            using (var portfolioDB = _dbHelperFactory.NewDBHelperPortfolio(requestedBy, requestedBy.ProjectID))\n+            {\n+                var resourceManager = new ResourceManager(portfolioDB, new DownloadRequestBase\n+                {\n+                    TableType = PrologTable.Types.Drawing\n+                });\n+\n+                var expectedFmsPath = Path.Combine(resourceManager.FMSDirectory, fileLink.FileGuid);\n+                File.Copy(stagedUploadPath, expectedFmsPath, overwrite: true);\n+\n+                var resource = new ResourceFile(fileLink);\n+                var resolved = await resource.ResolveAsync(resourceManager, CancellationToken.None);\n+\n+                using (new AssertionScope())\n+                {\n+                    resolved.Should().BeTrue(\"ResolveAsync must return true when the linked file exists in FMSDirectory\");\n+                    resource.FileServerLocation.Should().Be(expectedFmsPath);\n+                    File.Exists(resource.FileServerLocation).Should().BeTrue();\n+                    resource.FileZipPath.Should().NotBeNullOrWhiteSpace(\"zip folder path must be set for download packaging\");\n+                }\n+            }\n+        }\n+\n         // PS-61847\n         [TestMethod]\n         public async Task UserCanSeeExportedAnnotationsWithRecordAndFileLinksNoPrivateAnnotations()"
    },
    {
      "filename": "Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/ProjectExport/ProjectExportIntegrationTests.cs",
      "status": "modified",
      "additions": 114,
      "deletions": 0,
      "patch": "@@ -26,6 +26,12 @@ public override async Task CustomTestCleanupAsync()\n             await base.CustomTestCleanupAsync();\n \n             RemoveCreatedExportRequests(m_CreatedRequestIds);\n+\n+            foreach (var fileShare in m_AcquiredFileShares)\n+            {\n+                ReleaseUniqueFileShare(fileShare);\n+            }\n+            m_AcquiredFileShares.Clear();\n         }\n \n         #region Request Initiation\n@@ -1212,6 +1218,78 @@ public async Task RootFolderIsCreatedCorrectly()\n             Directory.Exists(folder).Should().BeTrue();\n         }\n \n+        // PS-69087 Phase 7: covers ProjectExportSystem.GetProjectExportPath (test-coverage.md #4)\n+        [TestMethod]\n+        public void GetProjectExportPathDoesNotCreateDirectoryWhenOkToCreateIfMissingFalse()\n+        {\n+            var fileShare = AcquireUniqueFileShare();\n+            try\n+            {\n+                var request = new ProjectExportRequest\n+                              {\n+                                  PortfolioId = m_oPortfolioId,\n+                                  FileShare = fileShare\n+                              };\n+\n+                var expected = new PortfolioExtract(m_oPortfolioId, fileShare).ProjectExportDirectory;\n+\n+                var actual = ProjectExportSystem.GetProjectExportPath(request);\n+\n+                using (new AssertionScope())\n+                {\n+                    actual.Should().Be(expected, \"static helper must compose ProjectExportDirectory from FileShare and PortfolioId\");\n+                    Directory.Exists(actual).Should().BeFalse(\"okToCreateIfMissing=false must not touch FileShare\");\n+                }\n+            }\n+            finally\n+            {\n+                ReleaseUniqueFileShare(fileShare);\n+            }\n+        }\n+\n+        // PS-69087 Phase 7: covers ProjectExportSystem.GetProjectExportPath create-on-demand path (test-coverage.md #4)\n+        [TestMethod]\n+        public void GetProjectExportPathCreatesDirectoryWhenOkToCreateIfMissingTrue()\n+        {\n+            var fileShare = AcquireUniqueFileShare();\n+            try\n+            {\n+                var request = new ProjectExportRequest\n+                              {\n+                                  PortfolioId = m_oPortfolioId,\n+                                  FileShare = fileShare\n+                              };\n+\n+                var expected = new PortfolioExtract(m_oPortfolioId, fileShare).ProjectExportDirectory;\n+\n+                Directory.Exists(expected).Should().BeFalse(\"test precondition: directory must be missing before the call\");\n+\n+                var actual = ProjectExportSystem.GetProjectExportPath(request, okToCreateIfMissing: true);\n+\n+                using (new AssertionScope())\n+                {\n+                    actual.Should().Be(expected, \"returned path must point at the ProjectExportDirectory\");\n+                    Directory.Exists(actual).Should().BeTrue(\"okToCreateIfMissing=true must create the missing directory on disk\");\n+                }\n+            }\n+            finally\n+            {\n+                ReleaseUniqueFileShare(fileShare);\n+            }\n+        }\n+\n+        // PS-69087 Phase 7: confirms the helper resolves to the live portfolio's ProjectExportDirectory\n+        [TestMethod]\n+        public void GetProjectExportPathResolvesToPortfolioExportDirectory()\n+        {\n+            var request = SaveRequest(GenerateRequest(), okToAddRequestedBy: true);\n+            m_CreatedRequestIds.Add(request.RecordId);\n+\n+            var actual = ProjectExportSystem.GetProjectExportPath(request);\n+\n+            actual.Should().Be(request.RequestedBy.User.CurrentPortfolio.ProjectExportDirectory, \"static helper must agree with the portfolio extract used at runtime\");\n+        }\n+\n #if false\n         [TestMethod]\n         public void TopLevelFoldersAreCreatedCorrectly()\n@@ -2976,6 +3054,42 @@ private class ProjectExportFailedRecord : KeyedRecordLite\n \n         #endregion // ProjectExportFile\n \n+        #region FileShare scratch directories (PS-69087 Phase 7)\n+\n+        private readonly List<string> m_AcquiredFileShares = new();\n+\n+        /// <summary>\n+        /// Returns a unique, empty directory that does <b>not</b> contain the portfolio's\n+        /// <see cref=\"PortfolioExtract.PortfolioBaseDirectory\"/> subtree yet, so\n+        /// <see cref=\"ProjectExportSystem.GetProjectExportPath\"/> can be exercised against\n+        /// a clean slate without colliding with the live FileShare provisioned by the test framework.\n+        /// </summary>\n+        private string AcquireUniqueFileShare()\n+        {\n+            var fileShare = Path.Combine(Path.GetTempPath(), \"PS-69087-Phase7-\" + Guid.NewGuid().ToString(\"N\"));\n+            Directory.CreateDirectory(fileShare);\n+            m_AcquiredFileShares.Add(fileShare);\n+\n+            return fileShare;\n+        }\n+\n+        private void ReleaseUniqueFileShare(string fileShare)\n+        {\n+            try\n+            {\n+                if (!string.IsNullOrEmpty(fileShare) && Directory.Exists(fileShare))\n+                {\n+                    Directory.Delete(fileShare, recursive: true);\n+                }\n+            }\n+            catch\n+            {\n+                // Best-effort cleanup; CustomTestCleanupAsync makes a final pass.\n+            }\n+        }\n+\n+        #endregion // FileShare scratch directories (PS-69087 Phase 7)\n+\n         #endregion // Helpers\n     }\n }\n\\ No newline at end of file"
    },
    {
      "filename": "docs/temp/tfs-prep-non-tc-files/PS-69087-fileshare-methods.playlist",
      "status": "added",
      "additions": 22,
      "deletions": 0,
      "patch": "@@ -0,0 +1,22 @@\n+<Playlist Version=\"2.0\">\n+  <!--\n+    PS-69087 Phase 7 — FileShare coverage for project export path, Excel error list, and ResourceFile.ResolveAsync.\n+    See test-coverage.md rows #4, #44, #109.\n+  -->\n+  <Rule Name=\"Includes\" Match=\"Any\">\n+\n+    <Rule Match=\"All\">\n+      <Property Name=\"Project\" Value=\"ProjectSight.Tests.ParallelIntegrationTests\" />\n+      <Property Name=\"Class\" Value=\"ProjectSight.Tests.IntegrationTests.ProjectExport.ProjectExportIntegrationTests\" />\n+    </Rule>\n+    <Rule Match=\"All\">\n+      <Property Name=\"Project\" Value=\"ProjectSight.Tests.ParallelIntegrationTests\" />\n+      <Property Name=\"Class\" Value=\"ProjectSight.Tests.IntegrationTests.Excel.Submittals.ExportImportScenarioTests\" />\n+    </Rule>\n+    <Rule Match=\"All\">\n+      <Property Name=\"Project\" Value=\"ProjectSight.Tests.ParallelIntegrationTests\" />\n+      <Property Name=\"Class\" Value=\"ProjectSight.Tests.IntegrationTests.PDF.AnnotationExporterTests\" />\n+    </Rule>\n+\n+  </Rule>\n+</Playlist>"
    },
    {
      "filename": "docs/temp/tfs-prep-non-tc-files/coverage-progress.md",
      "status": "added",
      "additions": 150,
      "deletions": 0,
      "patch": "@@ -0,0 +1,150 @@\n+# PS-69087 — Coverage Progress Tracker\n+\n+> Single source of truth for the PS-69087 FileShare coverage epic phase\n+> checklist. Each agent updates the relevant checkbox when their phase PR\n+> merges. Plan source: `C:\\Users\\acollin\\.cursor\\plans\\ps-69087_coverage_plan_86b29422.plan.md`.\n+>\n+> **Branch root:** `lucretia/TrimbleFileService` (per the plan; epic target).\n+> **Current orchestrator branch:** `wip/adam/tfc-prep-non-tc-files`.\n+\n+---\n+\n+## Phase Checklist (mirrors plan)\n+\n+### Phase 0 — Prerequisites (blocks Phase 8)\n+\n+- [x] `DMSSystem.GetFMSDirectory(int projectID)` overload — **resolved by build evidence (no Randy follow-up needed for the technical question; disposition still requires team input — see below)**\n+- [ ] `ConnectArchiveSystem.LocatePhysicalLinkedFile` / `LocateFileOrPhoto` (#19, #20) scope confirmation — **OPEN: needs Randy / epic owner**\n+- [ ] Minimum supported mobile version (PS-69093 follow-up note 6) — **OPEN: needs Randy / mobile team**\n+\n+### Phase 1 — UI controller + legacy mobile coverage (CRITICAL)\n+\n+- [ ] Phase 1 PR merged\n+\n+### Phase 2 — Trimble Connect reversal paths\n+\n+- [ ] Phase 2 PR merged\n+\n+### Phase 3 — SiteCapture BL surface (largest gap — 0 of 7 covered)\n+\n+- [ ] Phase 3 PR merged\n+\n+### Phase 4 — External file service (TFS migration surface)\n+\n+- [ ] Phase 4 PR merged\n+\n+### Phase 5 — Drawing/PDF FileShare ops\n+\n+- [ ] Phase 5 PR merged\n+\n+### Phase 6 — Org provisioning + admin/health endpoints\n+\n+- [ ] Phase 6 PR merged\n+\n+### Phase 7 — Project export niche helpers\n+\n+- [x] Phase 7 tests implemented — `ProjectExportSystem.GetProjectExportPath` (#4), `ExportManager.ExportErrorList` (#109), `ResourceFile.ResolveAsync` (#44) in `ProjectExportIntegrationTests`, `ExportImportScenarioTests`, `AnnotationExporterTests`\n+- [ ] Phase 7 PR merged\n+\n+### Phase 8 — Server daemon / search (BLOCKED by Phase 0)\n+\n+- [ ] Phase 0 confirms scope of #45/#64/#19/#20\n+- [ ] Phase 8 PR merged (or formally deferred in tracking doc)\n+\n+### Phase 9 — Harden Indirect → Direct for \"Both\" methods\n+\n+- [ ] Phase 9 PR merged\n+\n+### Phase 10 — Harden uploader pipeline assertions (Indirect → Direct)\n+\n+- [ ] Phase 10 PR merged\n+\n+---\n+\n+## Phase 0 — Findings & Open Questions\n+\n+### Resolved by code/build evidence — Prerequisite #1: `DMSSystem.GetFMSDirectory(int projectID)` overload\n+\n+**Status:** Technically resolved; disposition still requires a team decision.\n+\n+**Question (from plan):** Confirm whether the unresolved `DMSSystem.GetFMSDirectory(int projectID)` overload referenced by `SearchSystem.SearchPrologWebFolders` (#45) and `IndexProcess.StartWork` (#64) compiles, is dead code, or comes from an external assembly. **Block Phase 8 until resolved.**\n+\n+**Answer (from build evidence on this branch):** Both call sites are **dead code that does not compile** in the active build. No external assembly defines a `DMSSystem.GetFMSDirectory(int)` overload. There is therefore nothing to test for #45 / #64 in their current form.\n+\n+#### Evidence\n+\n+- The only definition of `DMSSystem` in the repo is `Core/Business Layer/DataAccess/DMSSystem.cs` (`public class DMSSystem : SystemBase` in namespace `ProjectSight.DataAccess`). It contains **no** `GetFMSDirectory` member of any signature; the only `GetFMSDirectory` mention inside `DMSSystem.cs` is the call `new ProjectSightDMSService(portfolioDB.RequestedByObject).GetFMSDirectory()` (instance, no-arg) at line 2658.\n+- The two existing `GetFMSDirectory` definitions live on `ProjectSightDMSService`:\n+    - `public string GetFMSDirectory()` (instance, no args) — `Core/Business Layer/DataAccess/Services/ProjectSightDMS/ProjectSightDMSService.cs:3480`\n+    - `public static string GetFMSDirectory(IDBHelper portfolioDB)` — same file, line 3521\n+    - **No `(int projectID)` overload exists on `ProjectSightDMSService` or anywhere else.**\n+- A whole-repo search for `GetFMSDirectory` finds zero matches under `Common/` (`GCCM.Common.SystemFramework`) and zero matches anywhere outside the two `*.cs` files listed above.\n+- `SearchSystem.cs` is **explicitly excluded from compilation** in the only csproj that contains it:\n+    - `Core/Business Layer/DataAccess/ProjectSight.BusinessLayer.DataAccess.csproj` line 27 — `<Compile Remove=\"SearchSystem.cs\" />`\n+    - No conditional `<Compile Include>` re-adds it elsewhere in the file.\n+- `IndexProcess.cs` lives in `Core/Servers/IndexServer/ProjectSight.Servers.IndexServer.csproj`. That csproj is **not referenced from any `*.sln` file** in the repository (`All.sln`, `WebOnly.sln`, `APIOnly.sln`, `ProjectExportServiceOnly.sln`, `ServerMonitorOnly.sln`, `WebUI.sln`, `Office365.sln`, `HPBudgetMapping.sln`, `C1Reporting.sln`, `QATestApp.sln` — none reference `IndexServer`). It also still targets `net4.7` / `LangVersion 5`, consistent with abandoned legacy code.\n+- `ITCMigrationReversalSystem.cs` and `Document Search\\SearchParameters.cs` are also `<Compile Remove>`'d in `Core/Common/Components/ProjectSight.Common.Components.csproj` (lines 38 and 39), reinforcing that the search/indexing surface is dormant on this branch.\n+- The only other consumer of `SearchSystem` is `Core/Web Layer/Unused/DMS/search.aspx.cs` — under the `Unused/` directory.\n+\n+#### Disposition still needed (lightweight Randy / epic-owner question)\n+\n+The technical \"compiles?\" question is answered. What remains is a **policy** call:\n+\n+> Phase 0 prereq #1 — `SearchSystem.SearchPrologWebFolders` (#45) and `IndexProcess.StartWork` (#64) are confirmed dead code on `wip/adam/tfc-prep-non-tc-files` (and on `lucretia/TrimbleFileService`, which has the same `<Compile Remove>` exclusions): `SearchSystem.cs` is `<Compile Remove>`'d in `ProjectSight.BusinessLayer.DataAccess.csproj`, and `ProjectSight.Servers.IndexServer.csproj` is not referenced from any `.sln`. The `DMSSystem.GetFMSDirectory(int)` overload that both files reference does not exist anywhere in source or in `GCCM.Common.SystemFramework`. **Question for Randy / the search-indexing owner:** Are these methods slated for revival as part of TFS prep, or should Phase 8's #45 / #64 be marked formally deferred in `coverage-progress.md` and removed from the playlist? If revival is planned, when, and which branch?\n+\n+This question is **non-blocking for Phases 1–7, 9, 10**. It only blocks Phase 8.\n+\n+### OPEN — Prerequisite #2: `ConnectArchiveSystem.LocatePhysicalLinkedFile` / `LocateFileOrPhoto` scope (#19, #20)\n+\n+**Status:** Cannot be resolved from code alone. Requires Randy / epic-owner input.\n+\n+**What code already tells us:**\n+\n+- Both methods are **private** on `ConnectArchiveSystem` (`Core/Business Layer/DataAccess/ConnectArchiveSystem.cs:267, :292`); only entry point is `FileRecoveryJobHandler` (a background job).\n+- They run **only in the TC-as-FMS scenario**: read from `FMSDirectory` / `PhotosDirectory` and push to Trimble Connect.\n+- Per `PS-69089-read-methods.md` (\"ConnectArchiveSystem — Not Obsolete\"), the class is actively called by `FileRecoveryJobHandler`; FileShare reads happen specifically in TCAsFMS scenarios.\n+- The per-task notes in `test-coverage.md` and `summary-of-methods.md` flag both as TC-recovery (Medium per the task's \"TC-exclusion guidance\").\n+\n+**Question to send to Randy / epic owner:**\n+\n+> PS-69087 Phase 0 prereq #2 — `ConnectArchiveSystem.LocatePhysicalLinkedFile` / `LocateFileOrPhoto` (test-coverage.md #19, #20) are TC-recovery-only paths reached via `FileRecoveryJobHandler`. They read from `FMSDirectory` / `PhotosDirectory` and push files to Trimble Connect. Per the per-task \"TC-exclusion guidance\" notes the original triage marked them Medium and out of the main coverage scope, but they are real FileShare reads that will become TFS reads after the migration. Should Phase 8 add a `FileRecoveryJobHandler`-driven test for these (in scope), or formally defer them as TC-only and document the deferral in `coverage-progress.md` (out of scope)?\n+\n+### OPEN — Prerequisite #3: Minimum supported mobile version (PS-69093 follow-up note 6)\n+\n+**Status:** Cannot be resolved from code alone. Requires Randy / mobile team input.\n+\n+**What code already tells us:**\n+\n+- `Core/API Layer/ProjectSight.WebAPI/Controllers/v1/SyncController.cs` source comments document **introduction** versions, not deprecation versions:\n+    - `UploadAttachment` (line 2627) — `// Used in Mobile 2.23 and forward`\n+    - `UploadFile` (line 2676) — `// Used in Mobile 2.4x and forward`\n+    - `UploadPiecemeal` (line 2720) — `// Used in Mobile 2.23 and forward`\n+    - `UploadAppend` (line 2821) — `// Used in Mobile 2.4x and forward`\n+- The PS-69093 follow-up note 6 references a stale `USED by 2.38 and older mobiles` comment dated 2026-01-01 that no longer matches what is in source. The current comments do not flag any endpoint as legacy.\n+- There is no `MinimumSupportedMobileVersion` config or guard in code that we could use to derive the answer.\n+\n+**Question to send to Randy / the mobile team:**\n+\n+> PS-69087 Phase 0 prereq #3 (per PS-69093 write-methods follow-up note 6) — `SyncController.UploadAttachment` (test-coverage.md #99 / PS-69093 row 47) and `UploadPiecemeal` (PS-69093 row 49) are tagged in source as \"Used in Mobile 2.23 and forward\". The newer pair `UploadFile` (#100) / `UploadAppend` (PS-69093 row 50) is tagged \"Used in Mobile 2.4x and forward\". What is the current minimum supported mobile client version in production? If the floor is at or above the version that introduced `UploadFile` / `UploadAppend`, the legacy `UploadAttachment` + `UploadPiecemeal` pair (and their `InProgressDirectory` chunking) can be removed, eliminating that surface from the TFS migration. If they must stay, Phase 1 (`Mobile/LegacyUploadsRetrievalTests.cs`) should add coverage for both endpoints rather than just the `uploads://` legacy path.\n+\n+This question is **non-blocking for Phase 1 implementation** — Phase 1 already plans `Mobile/LegacyUploadsRetrievalTests.cs` against `ExternalFileSystem.ConfirmLegacyFileInUploadsDirectoryAsync`. The mobile-version answer changes the *breadth* of Phase 1 / Phase 10, not the start gate. Phase 8 is the only phase explicitly gated.\n+\n+---\n+\n+## Related findings (informational, not Phase 0 prerequisites)\n+\n+These are tangential build-level observations surfaced while resolving prereq #1. They are **not Phase 0 blockers** but will affect Phases 2 and 8 when those agents pick up the work.\n+\n+- **`TCMigrationReversalSystem.cs` is also `<Compile Remove>`'d** in `Core/Business Layer/DataAccess/ProjectSight.BusinessLayer.DataAccess.csproj` (line 28), and `ITCMigrationReversalSystem.cs` is `<Compile Remove>`'d in `Core/Common/Components/ProjectSight.Common.Components.csproj` (line 38). On this branch, all of `TCMigrationReversalSystem.DownloadFileAsync` / `DownloadAndUpdateDMSFileAsync` / `FetchNonPersistedQueuedFileProperties` (test-coverage.md #90, #91, #54, #53) are uncompiled. **Phase 2 must verify the file is back in the build on whichever branch Phase 2 targets** before its tests can run; the canonical compile-include is presumably restored on `lucretia/TrimbleFileService` when the migration code lands. Phase 2 agent: confirm build on target branch first.\n+- The `<Compile Remove>` block also lists `MaterialSystem.cs`, `FACSystem.cs`, and `ERPSystemAvro.cs`, none of which are in the PS-69087 method list — included here for completeness only.\n+\n+---\n+\n+## Done definition (per phase)\n+\n+Reproduced from the plan for clarity:\n+\n+1. New/updated tests compile and pass locally.\n+2. The relevant `[ ]` checkbox in the plan and in this `coverage-progress.md` is flipped to `[x]`.\n+3. The test classes are listed in `PS-69087-fileshare-methods.playlist`.\n+4. PR is merged into `lucretia/TrimbleFileService` (or whichever branch the rest of PS-69087 targets at the time)."
    }
  ],
  "diff": "diff --git a/Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/Excel/Submittals/ExportImportScenarioTests.cs b/Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/Excel/Submittals/ExportImportScenarioTests.cs\nindex b9935d605b7..dcf306dd859 100644\n--- a/Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/Excel/Submittals/ExportImportScenarioTests.cs\n+++ b/Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/Excel/Submittals/ExportImportScenarioTests.cs\n@@ -179,6 +179,46 @@ public async Task ExportUpdateRowsWithErrorsImportFixImport()\n             // Check the results\n             AssertReturnStatusCounts(oSecondImportResult, 2, 0, 2, 0, 0);\n         }\n+\n+        // PS-69087 Phase 7: covers ExportManager.ExportErrorList (test-coverage.md #109)\n+        // Forces an import error then asserts the regenerated error-list Excel\n+        // landed in the portfolio's WebFileUploadDirectory.\n+        [TestMethod]\n+        public async Task ExportErrorListWritesExcelToWebFileUploadDirectory()\n+        {\n+            var exportedFile = (await ExportRecordsToExcelAsync(SUBMITTAL_TABLE_TYPE, new List<int>())).ExportedFileName;\n+\n+            var workbook = ExcelFile.Load(exportedFile, LoadOptions.XlsxDefault);\n+            var worksheet = GetWorksheet(workbook, SUBMITTAL_TABLE_TYPE);\n+            var columnIndices = GetColumnIndices(worksheet);\n+\n+            // Add one row with a subject but no required revision/description so the import will fail.\n+            worksheet.Rows.InsertEmpty(FIRST_DATA_ROW_INDEX);\n+            worksheet.Rows[FIRST_DATA_ROW_INDEX].Cells[columnIndices[\"subject\"]].Value = Generate.String(\"ErrorListSubject\");\n+            worksheet.Rows[FIRST_DATA_ROW_INDEX].Cells[columnIndices[\"workflowstatename\"]].Value = WorkflowStateNames.Submittal.OPEN;\n+\n+            workbook.Save(exportedFile, SaveOptions.XlsxDefault);\n+\n+            var importResult = await ImportRecordsFromExcelAsync(exportedFile, SUBMITTAL_TABLE_TYPE);\n+\n+            importResult.Should().NotBeNull();\n+            importResult.HasErrors.Should().BeTrue(\"test seeds invalid rows so the importer must surface errors\");\n+            importResult.ErrorsFileName.Should().NotBeNullOrWhiteSpace(\"ExportErrorList must report the regenerated workbook name\");\n+\n+            var webFileUploadDirectory = GetWebFileUploadDirectory(m_oPortfolioId);\n+            var errorListPath = Path.Combine(webFileUploadDirectory, importResult.ErrorsFileName);\n+\n+            using (new AssertionScope())\n+            {\n+                File.Exists(errorListPath).Should().BeTrue(\"ExportErrorList must persist the error workbook to WebFileUploadDirectory\");\n+                Path.GetExtension(errorListPath).Should().Be(Support.EXCEL_EXTENSION, \"regenerated workbook must keep the .xlsx extension produced by GemBox Save\");\n+            }\n+\n+            var errorWorkbook = ExcelFile.Load(errorListPath, LoadOptions.XlsxDefault);\n+            var errorWorksheet = GetWorksheet(errorWorkbook, SUBMITTAL_TABLE_TYPE);\n+            errorWorksheet.Should().NotBeNull(\"regenerated workbook must contain the submittal worksheet\");\n+            errorWorksheet.Columns[0].Hidden.Should().BeFalse(\"ExportErrorList must un-hide the feedback column for failed rows\");\n+        }\n     }\n }\n \ndiff --git a/Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/PDF/AnnotationExporterTests.cs b/Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/PDF/AnnotationExporterTests.cs\nindex 4af6b15eff7..99462dda426 100644\n--- a/Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/PDF/AnnotationExporterTests.cs\n+++ b/Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/PDF/AnnotationExporterTests.cs\n@@ -99,6 +99,42 @@ public async Task UserCanSeeExportedAnnotationsWithRecordAndFileLinks()\n             ValidateResultPDF(returnResult, NUMBER_OF_ANNOTATIONS);\n         }\n \n+        // PS-69087 Phase 7: covers ResourceFile.ResolveAsync (test-coverage.md #44)\n+        [TestMethod]\n+        public async Task ResourceFileResolveAsyncFindsLinkedFileInFMSDirectory()\n+        {\n+            var drawing = await CreatePublishedDrawingWithAnnotations(DRAWING_TO_UPLOAD, 1);\n+            drawing = await AddFileLinkToAnnotation(drawing, drawing.Annotations[0]);\n+\n+            var fileLink = drawing.FileLinks.Single(l => l.AnnotationGUID == drawing.Annotations[0].GUID);\n+            var stagedUploadPath = Path.Combine(GetWebFileUploadDirectory(m_oPortfolioId), fileLink.FileGuid);\n+            File.Exists(stagedUploadPath).Should().BeTrue(\"AddFileLinkToAnnotation must stage the file in WebFileUploadDirectory\");\n+\n+            var requestedBy = GenerateRequestedByProject();\n+\n+            using (var portfolioDB = _dbHelperFactory.NewDBHelperPortfolio(requestedBy, requestedBy.ProjectID))\n+            {\n+                var resourceManager = new ResourceManager(portfolioDB, new DownloadRequestBase\n+                {\n+                    TableType = PrologTable.Types.Drawing\n+                });\n+\n+                var expectedFmsPath = Path.Combine(resourceManager.FMSDirectory, fileLink.FileGuid);\n+                File.Copy(stagedUploadPath, expectedFmsPath, overwrite: true);\n+\n+                var resource = new ResourceFile(fileLink);\n+                var resolved = await resource.ResolveAsync(resourceManager, CancellationToken.None);\n+\n+                using (new AssertionScope())\n+                {\n+                    resolved.Should().BeTrue(\"ResolveAsync must return true when the linked file exists in FMSDirectory\");\n+                    resource.FileServerLocation.Should().Be(expectedFmsPath);\n+                    File.Exists(resource.FileServerLocation).Should().BeTrue();\n+                    resource.FileZipPath.Should().NotBeNullOrWhiteSpace(\"zip folder path must be set for download packaging\");\n+                }\n+            }\n+        }\n+\n         // PS-61847\n         [TestMethod]\n         public async Task UserCanSeeExportedAnnotationsWithRecordAndFileLinksNoPrivateAnnotations()\ndiff --git a/Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/ProjectExport/ProjectExportIntegrationTests.cs b/Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/ProjectExport/ProjectExportIntegrationTests.cs\nindex 3ed9b5c3059..b478c3eca70 100644\n--- a/Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/ProjectExport/ProjectExportIntegrationTests.cs\n+++ b/Core/Tests/ProjectSight.Tests.ParallelIntegrationTests/ProjectExport/ProjectExportIntegrationTests.cs\n@@ -26,6 +26,12 @@ public override async Task CustomTestCleanupAsync()\n             await base.CustomTestCleanupAsync();\n \n             RemoveCreatedExportRequests(m_CreatedRequestIds);\n+\n+            foreach (var fileShare in m_AcquiredFileShares)\n+            {\n+                ReleaseUniqueFileShare(fileShare);\n+            }\n+            m_AcquiredFileShares.Clear();\n         }\n \n         #region Request Initiation\n@@ -1212,6 +1218,78 @@ public async Task RootFolderIsCreatedCorrectly()\n             Directory.Exists(folder).Should().BeTrue();\n         }\n \n+        // PS-69087 Phase 7: covers ProjectExportSystem.GetProjectExportPath (test-coverage.md #4)\n+        [TestMethod]\n+        public void GetProjectExportPathDoesNotCreateDirectoryWhenOkToCreateIfMissingFalse()\n+        {\n+            var fileShare = AcquireUniqueFileShare();\n+            try\n+            {\n+                var request = new ProjectExportRequest\n+                              {\n+                                  PortfolioId = m_oPortfolioId,\n+                                  FileShare = fileShare\n+                              };\n+\n+                var expected = new PortfolioExtract(m_oPortfolioId, fileShare).ProjectExportDirectory;\n+\n+                var actual = ProjectExportSystem.GetProjectExportPath(request);\n+\n+                using (new AssertionScope())\n+                {\n+                    actual.Should().Be(expected, \"static helper must compose ProjectExportDirectory from FileShare and PortfolioId\");\n+                    Directory.Exists(actual).Should().BeFalse(\"okToCreateIfMissing=false must not touch FileShare\");\n+                }\n+            }\n+            finally\n+            {\n+                ReleaseUniqueFileShare(fileShare);\n+            }\n+        }\n+\n+        // PS-69087 Phase 7: covers ProjectExportSystem.GetProjectExportPath create-on-demand path (test-coverage.md #4)\n+        [TestMethod]\n+        public void GetProjectExportPathCreatesDirectoryWhenOkToCreateIfMissingTrue()\n+        {\n+            var fileShare = AcquireUniqueFileShare();\n+            try\n+            {\n+                var request = new ProjectExportRequest\n+                              {\n+                                  PortfolioId = m_oPortfolioId,\n+                                  FileShare = fileShare\n+                              };\n+\n+                var expected = new PortfolioExtract(m_oPortfolioId, fileShare).ProjectExportDirectory;\n+\n+                Directory.Exists(expected).Should().BeFalse(\"test precondition: directory must be missing before the call\");\n+\n+                var actual = ProjectExportSystem.GetProjectExportPath(request, okToCreateIfMissing: true);\n+\n+                using (new AssertionScope())\n+                {\n+                    actual.Should().Be(expected, \"returned path must point at the ProjectExportDirectory\");\n+                    Directory.Exists(actual).Should().BeTrue(\"okToCreateIfMissing=true must create the missing directory on disk\");\n+                }\n+            }\n+            finally\n+            {\n+                ReleaseUniqueFileShare(fileShare);\n+            }\n+        }\n+\n+        // PS-69087 Phase 7: confirms the helper resolves to the live portfolio's ProjectExportDirectory\n+        [TestMethod]\n+        public void GetProjectExportPathResolvesToPortfolioExportDirectory()\n+        {\n+            var request = SaveRequest(GenerateRequest(), okToAddRequestedBy: true);\n+            m_CreatedRequestIds.Add(request.RecordId);\n+\n+            var actual = ProjectExportSystem.GetProjectExportPath(request);\n+\n+            actual.Should().Be(request.RequestedBy.User.CurrentPortfolio.ProjectExportDirectory, \"static helper must agree with the portfolio extract used at runtime\");\n+        }\n+\n #if false\n         [TestMethod]\n         public void TopLevelFoldersAreCreatedCorrectly()\n@@ -2976,6 +3054,42 @@ private class ProjectExportFailedRecord : KeyedRecordLite\n \n         #endregion // ProjectExportFile\n \n+        #region FileShare scratch directories (PS-69087 Phase 7)\n+\n+        private readonly List<string> m_AcquiredFileShares = new();\n+\n+        /// <summary>\n+        /// Returns a unique, empty directory that does <b>not</b> contain the portfolio's\n+        /// <see cref=\"PortfolioExtract.PortfolioBaseDirectory\"/> subtree yet, so\n+        /// <see cref=\"ProjectExportSystem.GetProjectExportPath\"/> can be exercised against\n+        /// a clean slate without colliding with the live FileShare provisioned by the test framework.\n+        /// </summary>\n+        private string AcquireUniqueFileShare()\n+        {\n+            var fileShare = Path.Combine(Path.GetTempPath(), \"PS-69087-Phase7-\" + Guid.NewGuid().ToString(\"N\"));\n+            Directory.CreateDirectory(fileShare);\n+            m_AcquiredFileShares.Add(fileShare);\n+\n+            return fileShare;\n+        }\n+\n+        private void ReleaseUniqueFileShare(string fileShare)\n+        {\n+            try\n+            {\n+                if (!string.IsNullOrEmpty(fileShare) && Directory.Exists(fileShare))\n+                {\n+                    Directory.Delete(fileShare, recursive: true);\n+                }\n+            }\n+            catch\n+            {\n+                // Best-effort cleanup; CustomTestCleanupAsync makes a final pass.\n+            }\n+        }\n+\n+        #endregion // FileShare scratch directories (PS-69087 Phase 7)\n+\n         #endregion // Helpers\n     }\n }\n\\ No newline at end of file\ndiff --git a/docs/temp/tfs-prep-non-tc-files/PS-69087-fileshare-methods.playlist b/docs/temp/tfs-prep-non-tc-files/PS-69087-fileshare-methods.playlist\nnew file mode 100644\nindex 00000000000..e7c4b97e418\n--- /dev/null\n+++ b/docs/temp/tfs-prep-non-tc-files/PS-69087-fileshare-methods.playlist\n@@ -0,0 +1,22 @@\n+<Playlist Version=\"2.0\">\n+  <!--\n+    PS-69087 Phase 7 — FileShare coverage for project export path, Excel error list, and ResourceFile.ResolveAsync.\n+    See test-coverage.md rows #4, #44, #109.\n+  -->\n+  <Rule Name=\"Includes\" Match=\"Any\">\n+\n+    <Rule Match=\"All\">\n+      <Property Name=\"Project\" Value=\"ProjectSight.Tests.ParallelIntegrationTests\" />\n+      <Property Name=\"Class\" Value=\"ProjectSight.Tests.IntegrationTests.ProjectExport.ProjectExportIntegrationTests\" />\n+    </Rule>\n+    <Rule Match=\"All\">\n+      <Property Name=\"Project\" Value=\"ProjectSight.Tests.ParallelIntegrationTests\" />\n+      <Property Name=\"Class\" Value=\"ProjectSight.Tests.IntegrationTests.Excel.Submittals.ExportImportScenarioTests\" />\n+    </Rule>\n+    <Rule Match=\"All\">\n+      <Property Name=\"Project\" Value=\"ProjectSight.Tests.ParallelIntegrationTests\" />\n+      <Property Name=\"Class\" Value=\"ProjectSight.Tests.IntegrationTests.PDF.AnnotationExporterTests\" />\n+    </Rule>\n+\n+  </Rule>\n+</Playlist>\ndiff --git a/docs/temp/tfs-prep-non-tc-files/coverage-progress.md b/docs/temp/tfs-prep-non-tc-files/coverage-progress.md\nnew file mode 100644\nindex 00000000000..a5f0306aea5\n--- /dev/null\n+++ b/docs/temp/tfs-prep-non-tc-files/coverage-progress.md\n@@ -0,0 +1,150 @@\n+# PS-69087 — Coverage Progress Tracker\n+\n+> Single source of truth for the PS-69087 FileShare coverage epic phase\n+> checklist. Each agent updates the relevant checkbox when their phase PR\n+> merges. Plan source: `C:\\Users\\acollin\\.cursor\\plans\\ps-69087_coverage_plan_86b29422.plan.md`.\n+>\n+> **Branch root:** `lucretia/TrimbleFileService` (per the plan; epic target).\n+> **Current orchestrator branch:** `wip/adam/tfc-prep-non-tc-files`.\n+\n+---\n+\n+## Phase Checklist (mirrors plan)\n+\n+### Phase 0 — Prerequisites (blocks Phase 8)\n+\n+- [x] `DMSSystem.GetFMSDirectory(int projectID)` overload — **resolved by build evidence (no Randy follow-up needed for the technical question; disposition still requires team input — see below)**\n+- [ ] `ConnectArchiveSystem.LocatePhysicalLinkedFile` / `LocateFileOrPhoto` (#19, #20) scope confirmation — **OPEN: needs Randy / epic owner**\n+- [ ] Minimum supported mobile version (PS-69093 follow-up note 6) — **OPEN: needs Randy / mobile team**\n+\n+### Phase 1 — UI controller + legacy mobile coverage (CRITICAL)\n+\n+- [ ] Phase 1 PR merged\n+\n+### Phase 2 — Trimble Connect reversal paths\n+\n+- [ ] Phase 2 PR merged\n+\n+### Phase 3 — SiteCapture BL surface (largest gap — 0 of 7 covered)\n+\n+- [ ] Phase 3 PR merged\n+\n+### Phase 4 — External file service (TFS migration surface)\n+\n+- [ ] Phase 4 PR merged\n+\n+### Phase 5 — Drawing/PDF FileShare ops\n+\n+- [ ] Phase 5 PR merged\n+\n+### Phase 6 — Org provisioning + admin/health endpoints\n+\n+- [ ] Phase 6 PR merged\n+\n+### Phase 7 — Project export niche helpers\n+\n+- [x] Phase 7 tests implemented — `ProjectExportSystem.GetProjectExportPath` (#4), `ExportManager.ExportErrorList` (#109), `ResourceFile.ResolveAsync` (#44) in `ProjectExportIntegrationTests`, `ExportImportScenarioTests`, `AnnotationExporterTests`\n+- [ ] Phase 7 PR merged\n+\n+### Phase 8 — Server daemon / search (BLOCKED by Phase 0)\n+\n+- [ ] Phase 0 confirms scope of #45/#64/#19/#20\n+- [ ] Phase 8 PR merged (or formally deferred in tracking doc)\n+\n+### Phase 9 — Harden Indirect → Direct for \"Both\" methods\n+\n+- [ ] Phase 9 PR merged\n+\n+### Phase 10 — Harden uploader pipeline assertions (Indirect → Direct)\n+\n+- [ ] Phase 10 PR merged\n+\n+---\n+\n+## Phase 0 — Findings & Open Questions\n+\n+### Resolved by code/build evidence — Prerequisite #1: `DMSSystem.GetFMSDirectory(int projectID)` overload\n+\n+**Status:** Technically resolved; disposition still requires a team decision.\n+\n+**Question (from plan):** Confirm whether the unresolved `DMSSystem.GetFMSDirectory(int projectID)` overload referenced by `SearchSystem.SearchPrologWebFolders` (#45) and `IndexProcess.StartWork` (#64) compiles, is dead code, or comes from an external assembly. **Block Phase 8 until resolved.**\n+\n+**Answer (from build evidence on this branch):** Both call sites are **dead code that does not compile** in the active build. No external assembly defines a `DMSSystem.GetFMSDirectory(int)` overload. There is therefore nothing to test for #45 / #64 in their current form.\n+\n+#### Evidence\n+\n+- The only definition of `DMSSystem` in the repo is `Core/Business Layer/DataAccess/DMSSystem.cs` (`public class DMSSystem : SystemBase` in namespace `ProjectSight.DataAccess`). It contains **no** `GetFMSDirectory` member of any signature; the only `GetFMSDirectory` mention inside `DMSSystem.cs` is the call `new ProjectSightDMSService(portfolioDB.RequestedByObject).GetFMSDirectory()` (instance, no-arg) at line 2658.\n+- The two existing `GetFMSDirectory` definitions live on `ProjectSightDMSService`:\n+    - `public string GetFMSDirectory()` (instance, no args) — `Core/Business Layer/DataAccess/Services/ProjectSightDMS/ProjectSightDMSService.cs:3480`\n+    - `public static string GetFMSDirectory(IDBHelper portfolioDB)` — same file, line 3521\n+    - **No `(int projectID)` overload exists on `ProjectSightDMSService` or anywhere else.**\n+- A whole-repo search for `GetFMSDirectory` finds zero matches under `Common/` (`GCCM.Common.SystemFramework`) and zero matches anywhere outside the two `*.cs` files listed above.\n+- `SearchSystem.cs` is **explicitly excluded from compilation** in the only csproj that contains it:\n+    - `Core/Business Layer/DataAccess/ProjectSight.BusinessLayer.DataAccess.csproj` line 27 — `<Compile Remove=\"SearchSystem.cs\" />`\n+    - No conditional `<Compile Include>` re-adds it elsewhere in the file.\n+- `IndexProcess.cs` lives in `Core/Servers/IndexServer/ProjectSight.Servers.IndexServer.csproj`. That csproj is **not referenced from any `*.sln` file** in the repository (`All.sln`, `WebOnly.sln`, `APIOnly.sln`, `ProjectExportServiceOnly.sln`, `ServerMonitorOnly.sln`, `WebUI.sln`, `Office365.sln`, `HPBudgetMapping.sln`, `C1Reporting.sln`, `QATestApp.sln` — none reference `IndexServer`). It also still targets `net4.7` / `LangVersion 5`, consistent with abandoned legacy code.\n+- `ITCMigrationReversalSystem.cs` and `Document Search\\SearchParameters.cs` are also `<Compile Remove>`'d in `Core/Common/Components/ProjectSight.Common.Components.csproj` (lines 38 and 39), reinforcing that the search/indexing surface is dormant on this branch.\n+- The only other consumer of `SearchSystem` is `Core/Web Layer/Unused/DMS/search.aspx.cs` — under the `Unused/` directory.\n+\n+#### Disposition still needed (lightweight Randy / epic-owner question)\n+\n+The technical \"compiles?\" question is answered. What remains is a **policy** call:\n+\n+> Phase 0 prereq #1 — `SearchSystem.SearchPrologWebFolders` (#45) and `IndexProcess.StartWork` (#64) are confirmed dead code on `wip/adam/tfc-prep-non-tc-files` (and on `lucretia/TrimbleFileService`, which has the same `<Compile Remove>` exclusions): `SearchSystem.cs` is `<Compile Remove>`'d in `ProjectSight.BusinessLayer.DataAccess.csproj`, and `ProjectSight.Servers.IndexServer.csproj` is not referenced from any `.sln`. The `DMSSystem.GetFMSDirectory(int)` overload that both files reference does not exist anywhere in source or in `GCCM.Common.SystemFramework`. **Question for Randy / the search-indexing owner:** Are these methods slated for revival as part of TFS prep, or should Phase 8's #45 / #64 be marked formally deferred in `coverage-progress.md` and removed from the playlist? If revival is planned, when, and which branch?\n+\n+This question is **non-blocking for Phases 1–7, 9, 10**. It only blocks Phase 8.\n+\n+### OPEN — Prerequisite #2: `ConnectArchiveSystem.LocatePhysicalLinkedFile` / `LocateFileOrPhoto` scope (#19, #20)\n+\n+**Status:** Cannot be resolved from code alone. Requires Randy / epic-owner input.\n+\n+**What code already tells us:**\n+\n+- Both methods are **private** on `ConnectArchiveSystem` (`Core/Business Layer/DataAccess/ConnectArchiveSystem.cs:267, :292`); only entry point is `FileRecoveryJobHandler` (a background job).\n+- They run **only in the TC-as-FMS scenario**: read from `FMSDirectory` / `PhotosDirectory` and push to Trimble Connect.\n+- Per `PS-69089-read-methods.md` (\"ConnectArchiveSystem — Not Obsolete\"), the class is actively called by `FileRecoveryJobHandler`; FileShare reads happen specifically in TCAsFMS scenarios.\n+- The per-task notes in `test-coverage.md` and `summary-of-methods.md` flag both as TC-recovery (Medium per the task's \"TC-exclusion guidance\").\n+\n+**Question to send to Randy / epic owner:**\n+\n+> PS-69087 Phase 0 prereq #2 — `ConnectArchiveSystem.LocatePhysicalLinkedFile` / `LocateFileOrPhoto` (test-coverage.md #19, #20) are TC-recovery-only paths reached via `FileRecoveryJobHandler`. They read from `FMSDirectory` / `PhotosDirectory` and push files to Trimble Connect. Per the per-task \"TC-exclusion guidance\" notes the original triage marked them Medium and out of the main coverage scope, but they are real FileShare reads that will become TFS reads after the migration. Should Phase 8 add a `FileRecoveryJobHandler`-driven test for these (in scope), or formally defer them as TC-only and document the deferral in `coverage-progress.md` (out of scope)?\n+\n+### OPEN — Prerequisite #3: Minimum supported mobile version (PS-69093 follow-up note 6)\n+\n+**Status:** Cannot be resolved from code alone. Requires Randy / mobile team input.\n+\n+**What code already tells us:**\n+\n+- `Core/API Layer/ProjectSight.WebAPI/Controllers/v1/SyncController.cs` source comments document **introduction** versions, not deprecation versions:\n+    - `UploadAttachment` (line 2627) — `// Used in Mobile 2.23 and forward`\n+    - `UploadFile` (line 2676) — `// Used in Mobile 2.4x and forward`\n+    - `UploadPiecemeal` (line 2720) — `// Used in Mobile 2.23 and forward`\n+    - `UploadAppend` (line 2821) — `// Used in Mobile 2.4x and forward`\n+- The PS-69093 follow-up note 6 references a stale `USED by 2.38 and older mobiles` comment dated 2026-01-01 that no longer matches what is in source. The current comments do not flag any endpoint as legacy.\n+- There is no `MinimumSupportedMobileVersion` config or guard in code that we could use to derive the answer.\n+\n+**Question to send to Randy / the mobile team:**\n+\n+> PS-69087 Phase 0 prereq #3 (per PS-69093 write-methods follow-up note 6) — `SyncController.UploadAttachment` (test-coverage.md #99 / PS-69093 row 47) and `UploadPiecemeal` (PS-69093 row 49) are tagged in source as \"Used in Mobile 2.23 and forward\". The newer pair `UploadFile` (#100) / `UploadAppend` (PS-69093 row 50) is tagged \"Used in Mobile 2.4x and forward\". What is the current minimum supported mobile client version in production? If the floor is at or above the version that introduced `UploadFile` / `UploadAppend`, the legacy `UploadAttachment` + `UploadPiecemeal` pair (and their `InProgressDirectory` chunking) can be removed, eliminating that surface from the TFS migration. If they must stay, Phase 1 (`Mobile/LegacyUploadsRetrievalTests.cs`) should add coverage for both endpoints rather than just the `uploads://` legacy path.\n+\n+This question is **non-blocking for Phase 1 implementation** — Phase 1 already plans `Mobile/LegacyUploadsRetrievalTests.cs` against `ExternalFileSystem.ConfirmLegacyFileInUploadsDirectoryAsync`. The mobile-version answer changes the *breadth* of Phase 1 / Phase 10, not the start gate. Phase 8 is the only phase explicitly gated.\n+\n+---\n+\n+## Related findings (informational, not Phase 0 prerequisites)\n+\n+These are tangential build-level observations surfaced while resolving prereq #1. They are **not Phase 0 blockers** but will affect Phases 2 and 8 when those agents pick up the work.\n+\n+- **`TCMigrationReversalSystem.cs` is also `<Compile Remove>`'d** in `Core/Business Layer/DataAccess/ProjectSight.BusinessLayer.DataAccess.csproj` (line 28), and `ITCMigrationReversalSystem.cs` is `<Compile Remove>`'d in `Core/Common/Components/ProjectSight.Common.Components.csproj` (line 38). On this branch, all of `TCMigrationReversalSystem.DownloadFileAsync` / `DownloadAndUpdateDMSFileAsync` / `FetchNonPersistedQueuedFileProperties` (test-coverage.md #90, #91, #54, #53) are uncompiled. **Phase 2 must verify the file is back in the build on whichever branch Phase 2 targets** before its tests can run; the canonical compile-include is presumably restored on `lucretia/TrimbleFileService` when the migration code lands. Phase 2 agent: confirm build on target branch first.\n+- The `<Compile Remove>` block also lists `MaterialSystem.cs`, `FACSystem.cs`, and `ERPSystemAvro.cs`, none of which are in the PS-69087 method list — included here for completeness only.\n+\n+---\n+\n+## Done definition (per phase)\n+\n+Reproduced from the plan for clarity:\n+\n+1. New/updated tests compile and pass locally.\n+2. The relevant `[ ]` checkbox in the plan and in this `coverage-progress.md` is flipped to `[x]`.\n+3. The test classes are listed in `PS-69087-fileshare-methods.playlist`.\n+4. PR is merged into `lucretia/TrimbleFileService` (or whichever branch the rest of PS-69087 targets at the time).\n"
}